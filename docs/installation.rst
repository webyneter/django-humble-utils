============
Installation
============

At the command line::

    $ easy_install django-humble-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-humble-utils
    $ pip install django-humble-utils
